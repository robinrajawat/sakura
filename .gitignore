# Stray manual backups — git history is the backup, don't commit copies of index.html
*backup*.html
*.bak

# Phase 0 dev tooling (docs/architecture-plan.md) — build artifacts and dependencies,
# none of which are part of the deployed app (that's still the hand-maintained root
# index.html/hub.html, served as-is)
node_modules/
dist/
playwright-report/
test-results/
*.tsbuildinfo
