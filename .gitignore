# Stray manual backups — git history is the backup, don't commit copies of index.html
*backup*.html
*.bak

# Build artifacts and dependencies, none of which are committed, across both npm
# workspaces (legacy/, web/) and the root. index.html/hub.html (legacy/) are still
# hand-maintained; as of Stage 2 they're published via CI-built legacy/dist/ (deploy.yml),
# not served directly from this branch. See docs/history/web-migration/framework-migration-plan.md for the
# legacy/+web/ workspace split.
node_modules/
dist/
playwright-report/
test-results/
*.tsbuildinfo
