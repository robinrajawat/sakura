# Stray manual backups — git history is the backup, don't commit copies of index.html
*backup*.html
*.bak

# Phase 0 dev tooling (docs/architecture-plan.md) — build artifacts and dependencies,
# none of which are committed. index.html/hub.html are still hand-maintained; as of Stage 2
# they're published via CI-built dist/ (deploy.yml), not served directly from this branch.
node_modules/
dist/
playwright-report/
test-results/
*.tsbuildinfo
