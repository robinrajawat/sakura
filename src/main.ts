// Phase 0 placeholder. Not imported by index.html or hub.html yet — those stay exactly as
// they are (hand-maintained, single-file, unchanged) until Phase 1 begins actual code
// extraction per docs/architecture-plan.md. This file exists solely so `npm run typecheck`
// and `npm run build` have something real to compile, proving the pipeline itself works
// before any application logic moves into it.
export {};
