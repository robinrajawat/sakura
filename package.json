{
  "name": "sakura",
  "private": true,
  "version": "0.0.0",
  "description": "Sakura — outliner app. This package.json is DEV TOOLING ONLY (Phase 0 of the modularization plan, see docs/architecture-plan.md). It does not change how the app is deployed today: index.html and hub.html at the repo root remain the live, hand-maintained production files served directly by GitHub Pages. Nothing here runs in production yet.",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "generate": "node scripts/generate-index-blocks.mjs",
    "generate:verify": "node scripts/generate-index-blocks.mjs --verify",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "lint": "eslint src tests --ext .ts,.js",
    "test:unit": "vitest run",
    "test:e2e": "playwright test",
    "test": "npm run test:unit && npm run test:e2e"
  },
  "devDependencies": {
    "@playwright/test": "1.56.0",
    "@typescript-eslint/eslint-plugin": "^8.0.0",
    "@typescript-eslint/parser": "^8.0.0",
    "eslint": "^9.0.0",
    "typescript": "^5.5.0",
    "vite": "^5.4.0",
    "vitest": "^2.0.0"
  }
}
